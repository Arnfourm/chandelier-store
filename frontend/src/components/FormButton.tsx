export function FormButton({ name }) {
    return (
        <button className="w-[800px] h-[80px] bg-stone-900 text-neutral-100 font-semibold text-2xl cursor-pointer">
            {name}
        </button>
    );
}
